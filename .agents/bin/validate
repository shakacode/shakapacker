#!/usr/bin/env bash
# Pre-push gate: lint + test (run before pushing).
set -euo pipefail
root="$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
cd "$root"
"$root/.agents/bin/lint"
bundle exec ruby "$root/.agents/bin/merge-readiness-check-test.rb"
"$root/.agents/bin/test"
