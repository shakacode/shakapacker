# Generated by shaka seam migrate.
---
version: 1
base_branch: main
review:
  # `none` means no Shaka CI review job is configured; AGENTS.md and live GitHub rules still require the full merge gate and any required approval.
  required: none
merge:
  preference: ask
repo_prefix: SP
