{
  "scripts": {
    "setup": "./conductor-setup.sh",
    "run": "bin/conductor-exec bundle exec rake run_spec:gem",
    "test": "bin/conductor-exec bundle exec rake run_spec:gem",
    "lint": "bin/conductor-exec bundle exec rubocop && bin/conductor-exec yarn lint",
    "archive": ""
  }
}
